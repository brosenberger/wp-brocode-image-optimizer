parameters:
    level: 6
    paths:
        - brocode-image-optimizer.php
    excludePaths:
        - vendor
        - tests
        - node_modules(?)
    bootstrapFiles:
        # WP_CLI has no official PHPStan stubs; class_exists() guards runtime use.
        - phpstan-stubs.php
