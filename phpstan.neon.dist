parameters:
    level: 6
    paths:
        - brocode-image-optimizer.php
    excludePaths:
        - vendor
        - tests
        - node_modules(?)
    stubFiles:
        # WP_CLI has no official PHPStan stubs; usage is guarded by class_exists() at runtime.
        - phpstan-stubs.php
